version: 1
suite: "dsl_fail_order"
model: "dummy"
tests:
  - id: "dsl_fail_2"
    input:
      prompt: "test"
    expected:
      type: sequence_valid
      rules:
        - type: before
          first: "shutdown"
          then: "init"
