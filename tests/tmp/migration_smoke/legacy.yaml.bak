version: 1
suite: migration-test
model: fake-gpt
tests:
  - id: legacy-test
    input: { prompt: "test" }
    expected:
      type: args_valid
      policy: policies/args.yaml
  - id: legacy-sequence
    input: { prompt: "test" }
    expected:
      type: sequence_valid
      policy: policies/sequence.yaml
