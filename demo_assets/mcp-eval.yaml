configVersion: 1
suite: mcp-basics

tests:
  - id: args_valid
    metric: args_valid

  - id: sequence_valid
    metric: sequence_valid
    rules:
      - type: require
        tool: get_weather
      - type: before
        first: get_weather
        then: deploy_service

  - id: tool_blocklist
    metric: tool_blocklist
    block: [admin_delete]
